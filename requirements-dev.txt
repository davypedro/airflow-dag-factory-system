# ============================================================
#  Dependências de desenvolvimento local
# ============================================================
#  Compatível com Python 3.13+.
#  Instale no ambiente virtual:
#    .venv/Scripts/pip install -r requirements-dev.txt  (Windows)
#    .venv/bin/pip install -r requirements-dev.txt      (Linux/Mac)
#
#  Não inclui providers do Airflow (eles rodam no Docker).
#  Os operadores customizados são testados com mocks.
# ============================================================

# ── Data manipulation ──────────────────────────────────────
pandas>=2.3.0                  # 2.3+ tem suporte a Python 3.13
pyarrow>=17.0.0

# ── Config / serialization ─────────────────────────────────
pyyaml>=6.0.1

# ── Testing ────────────────────────────────────────────────
pytest>=8.2.0
pytest-cov>=5.0.0
pytest-mock>=3.14.0

# ── Qualidade de código ────────────────────────────────────
ruff>=0.5.0                    # linter + formatter
mypy>=1.10.0                   # type checking
